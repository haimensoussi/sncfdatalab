name: 🚀 Deploy Notebooks to Databricks PROD (from main)

on:
  push:
    branches:
      - release
      - prod

jobs:
  deploy-to-prod:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Databricks CLI
        run: pip install databricks-cli

      # Vérification des secrets PROD
      - name: 🧩 Check secrets presence
        env:
          DATABRICKS_HOST: https://adb-690688979154426.6.azuredatabricks.net/
          DATABRICKS_TOKEN: dapi66fd064472135d2030cf4e094435f203
        run: |
          if [ -z "$DATABRICKS_HOST" ]; then
            echo "❌ ERREUR: DATABRICKS_HOST_PROD est vide !"
            exit 1
          fi
          if [ -z "$DATABRICKS_TOKEN" ]; then
            echo "❌ ERREUR: DATABRICKS_TOKEN_PROD est vide !"
            exit 1
          fi
          echo "✅ Secrets PROD correctement injectés."

      # Déploiement vers PROD
      - name: 🚀 Deploy notebooks to Databricks PROD
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST_PROD }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN_PROD }}
        run: |
          echo "Connexion à Databricks PROD..."
          databricks workspace ls /
          echo "✅ Connexion OK"

          echo "Déploiement des notebooks vers /Shared/sncfdatalab_prod ..."
          databricks workspace import_dir notebooks /Shared/sncfdatalab_prod --overwrite
          echo "✅ Déploiement terminé avec succès !"
