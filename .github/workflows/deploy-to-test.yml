name: 🚀 Deploy Notebooks to Databricks TEST (from main)

on:
  push:
    branches:
      - main

jobs:
  deploy-to-test:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------------------------------------
      # 1️⃣ Cloner le dépôt GitHub
      # ----------------------------------------------------------
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # ----------------------------------------------------------
      # 2️⃣ Installer Python et le Databricks CLI
      # ----------------------------------------------------------
      - name: ⚙️ Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Databricks CLI
        run: |
          pip install databricks-cli

      # ----------------------------------------------------------
      # 3️⃣ Tester la connexion et déployer les notebooks
      # ----------------------------------------------------------
      - name: 🚀 Deploy notebooks to Databricks TEST
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST_TEST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN_TEST }}
        run: |
          echo "Connexion à Databricks TEST..."
          databricks workspace ls /
          echo "✅ Connexion OK"

          echo "Déploiement des notebooks vers /Shared/sncfdatalab ..."
          databricks workspace import_dir notebooks /Shared/sncfdatalab --overwrite
          echo "✅ Déploiement terminé avec succès !"
