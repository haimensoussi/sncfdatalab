name: 🚀 Deploy Notebooks to Databricks TEST (from main)

on:
  push:
    branches:
      - main

jobs:
  deploy-to-test:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------------------------------------
      # 1️⃣ Cloner le dépôt
      # ----------------------------------------------------------
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # ----------------------------------------------------------
      # 2️⃣ Installer Python et Databricks CLI
      # ----------------------------------------------------------
      - name: ⚙️ Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Databricks CLI
        run: |
          pip install databricks-cli

      # ----------------------------------------------------------
      # 3️⃣ Authentification Databricks TEST
      # ----------------------------------------------------------
      - name: 🔐 Configure Databricks CLI for TEST
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST_TEST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN_TEST }}
        run: |
          mkdir -p ~/.databricks
          echo "[DEFAULT]" > ~/.databricks/config
          echo "host = $DATABRICKS_HOST" >> ~/.databricks/config
          echo "token = $DATABRICKS_TOKEN" >> ~/.databricks/config
          echo "✅ Authentifié sur Databricks TEST"

      # ----------------------------------------------------------
      # 4️⃣ Déploiement des notebooks vers Databricks TEST
      # ----------------------------------------------------------
      - name: 🚀 Deploy notebooks to TEST
        run: |
          echo "Déploiement des notebooks dans /Shared/sncfdatalab ..."
          databricks workspace import_dir notebooks /Shared/sncfdatalab --overwrite
          echo "✅ Notebooks déployés avec succès sur Databricks TEST !"
